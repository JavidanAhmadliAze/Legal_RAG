# ---------------------------------------------------------------------------
# Secrets — NEVER bake into an image
# ---------------------------------------------------------------------------
.env
.env.*
!.env.example
*.env
*.envrc
.envrc

.secrets
.secrets/
secrets/
*secret*
*secrets*
*.secret
*.secrets
credentials
credentials.*
*.credentials
*.credentials.json
service-account*.json
gcp-service-account*.json
google-credentials*.json
aws-credentials
.aws/
.boto
auth.json

*.token
*.tokens
*token.txt
hf_token*
.hf_token
huggingface_token*
*api_key*
*api-key*

*.pem
*.key
*.cer
*.crt
*.p12
*.pfx
*.jks
*.keystore
id_rsa
id_rsa.*
id_ed25519
id_ed25519.*
.ssh/

*.pgpass
.pgpass
.my.cnf

# ---------------------------------------------------------------------------
# Git / VCS
# ---------------------------------------------------------------------------
.git
.gitignore
.gitattributes
.github/

# ---------------------------------------------------------------------------
# Project data (large / regenerable)
# ---------------------------------------------------------------------------
data/
logs/
*.log
*.log.*

# ---------------------------------------------------------------------------
# Python build / cache artefacts
# ---------------------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.egg
*.egg-info/
dist/
build/
.eggs/
*.so

# Virtual envs (rebuild inside the image)
.venv/
venv/
env/
ENV/

# Tooling caches
.ruff_cache/
.pytest_cache/
.mypy_cache/
.tox/
.coverage
.coverage.*
htmlcov/
.cache
.hypothesis/

# Notebooks
.ipynb_checkpoints/

# ---------------------------------------------------------------------------
# Tests (not needed in production images)
# ---------------------------------------------------------------------------
tests/

# ---------------------------------------------------------------------------
# Documentation / repo metadata
# ---------------------------------------------------------------------------
README.md
*.md
docs/
LICENSE

# ---------------------------------------------------------------------------
# Docker meta (don't COPY these into the image)
# ---------------------------------------------------------------------------
Dockerfile
Dockerfile.*
.dockerignore
docker-compose.yml
docker-compose.*.yml

# ---------------------------------------------------------------------------
# Airflow runtime state
# ---------------------------------------------------------------------------
airflow/logs/
airflow/plugins/
airflow.db
airflow.cfg
unittests.cfg
webserver_config.py
.docker/

# ---------------------------------------------------------------------------
# IDE / Editor
# ---------------------------------------------------------------------------
.idea/
.vscode/
*.swp
*.swo
*.swn
*.bak
*~
.DS_Store
Thumbs.db
.codex
.claude/

# ---------------------------------------------------------------------------
# Local scratch
# ---------------------------------------------------------------------------
/tmp/
tmp/
scratch/
*.tmp

# ---------------------------------------------------------------------------
# Tracing / monitoring artefacts
# ---------------------------------------------------------------------------
phoenix.db
phoenix-data/
otel-traces/
